import React from "react"
import { graphql, StaticQuery } from 'gatsby'

export default () => (
  <div id="panu" class="panu" style = {{ height: 2000, marginTop: 0, border: 0, paddingTop: 0 }} >
  <p>Onneksi olkoon Panu!</p>
  </div>
)
